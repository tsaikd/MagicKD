把history放進enable class
取消資料夾時findpic也要取消
移動btn加tooltip

紀錄讀取失敗次數(順便可以刪除或查看)
圖片讀取錯誤log

重新啟動
設假桌布

整合BindWinKey

OnSize

螢幕解析度(ku)

bug:
	cache num 的部份不同步
	Release Version Property: Runtime Library 不能用 /MD 要用 /MDd , 不然中文字在 ListCtrl中顯示有問題
	Release 執行一段時間後關閉會 memmory leak